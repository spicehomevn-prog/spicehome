Small square chip for room highlights, amenities, and metadata.

```jsx
<Tag>Balcony access</Tag>
<Tag tone="accent">Private balcony</Tag>
```

`default` is a hairline-bordered muted chip on cream; `accent` is a soft terracotta fill. Group several with a flex `gap`. Always square.
