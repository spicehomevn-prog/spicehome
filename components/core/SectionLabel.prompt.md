The terracotta uppercase eyebrow that introduces nearly every section heading.

```jsx
<SectionLabel>About SpiceHome</SectionLabel>
<h2 style={{ fontFamily: 'var(--font-display)' }}>Not just a place to stay</h2>
```

`tone="light"` over dark/photo bands, `tone="muted"` for quieter contexts. Always pair it directly above a Playfair heading.
